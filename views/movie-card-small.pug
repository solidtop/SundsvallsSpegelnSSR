li(class="movie small") 
    a(class="movie-link", href=`/movies/${movie.id}`)
    img(class="movie__poster", src=movie.attributes.image.url, alt=movie.attributes.title)
    h3(class="movie__title")=movie.attributes.title